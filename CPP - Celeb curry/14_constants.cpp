#include <iostream>
#define X 10 // macro type of contants
using std::cout;

// symbolic constants
int main()
{
    const int x = 5; // when defined as constant variable values cannot be overwritten

    // secound type of enum type of constants
    enum { y = 100 }
}
